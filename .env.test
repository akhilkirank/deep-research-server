# Test Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=test

# Logging Configuration
# Set to error to reduce noise during tests
LOG_LEVEL=error
# Use pretty format for better readability in test output
LOG_FORMAT=pretty

# API Keys (mock values for testing)
GOOGLE_GENERATIVE_AI_API_KEY=test_google_api_key
TAVILY_API_KEY=test_tavily_api_key
OPENROUTER_API_KEY=test_openrouter_api_key

# API Base URLs (mock values for testing)
GOOGLE_GENERATIVE_AI_API_BASE_URL=https://generativelanguage.googleapis.com
TAVILY_API_BASE_URL=https://api.tavily.com
OPENROUTER_API_BASE_URL=https://openrouter.ai/api

# Default Providers
DEFAULT_LLM_PROVIDER=google
DEFAULT_SEARCH_PROVIDER=tavily

# Default Models
DEFAULT_THINKING_MODEL=gemini-2.0-flash-thinking-exp-01-21
DEFAULT_NETWORKING_MODEL=gemini-2.0-flash-001
DEFAULT_OPENROUTER_MODEL=anthropic/claude-3-opus:beta

# Test Configuration
TEST_TIMEOUT=30000
DEBUG_TESTS=false
